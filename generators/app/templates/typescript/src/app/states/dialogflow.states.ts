import { DialogFlowPlatform } from "voxa";

// prettier-ignore
// eslint-disable-next-line no-unused-vars
export function register(app: DialogFlowPlatform) {
  // eslint-disable-next-line no-empty-function
};
